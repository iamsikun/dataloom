experiment_id: expB_causal
profile: smoke
schema_version: 1
seed_root: 20260502

n_grid: [200, 500, 1000]
m_factor_grid: [1, 5]

population_size: 5000
true_ate: 4.0
population_seed: 0

constants: {}

replications:
  default: 50

estimators:
  - real_only_diff_in_means
  - real_only_aipw
  - synthetic_only_full_calibration
  - naive_pooling
  - corrected_adaptive_gn
  - safe_corrected_adaptive_gn
  - validation_debiased_gn
  - fixed_half_split_plugin_alpha
  - old_fixed_share_plugin_alpha

parallel:
  unit: cell
  n_jobs: -1
  backend: loky

output:
  results_root: results/
